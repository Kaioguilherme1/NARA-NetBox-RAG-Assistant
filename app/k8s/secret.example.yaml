# NAO commite segredos reais. Este arquivo e apenas um template de referencia.
# Crie o Secret a partir do seu .env:
#   kubectl -n agente-viagem create secret generic app-secrets --from-env-file=.env
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: agente-viagem
type: Opaque
stringData:
  OPENAI_API_KEY: "coloque-sua-chave-do-foundry"
  OPENAI_BASE_URL: "https://SEU-RECURSO.cognitiveservices.azure.com/openai/v1/"
  MODELO_CHAT: "gpt-5.4"
  MODELO_EMBED: "text-embedding-3-small"
  DB_URI: "postgresql://postgres:postgres@postgres:5432/rag"
